import { ProtectedImagePipe } from './protected-image.pipe';

describe('ProtectedImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProtectedImagePipe();
    expect(pipe).toBeTruthy();
  });
});
